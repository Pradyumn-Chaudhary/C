# include<stdio.h>
//area of square
int main(){

    float side;
    printf("enter side of square");
    scanf("%f",&side);

    // int Area = side * side;
    // printf("Area of a square is: %f \n",Area)
    printf("Area of square is: %f \n",side*side);

    return 0;
}