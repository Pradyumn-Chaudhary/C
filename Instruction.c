# include<stdio.h>
# include<math.h>
int main(){

    // int b , c;
    // b = 1 ,c = 2;

    int b = 1,c = 2;
    int a = pow(b,c);
    printf("b to the power c is equal to %d \n",a);

    int z = (int) 1.99999;
    printf("z = %d",z);

    return 0;
}